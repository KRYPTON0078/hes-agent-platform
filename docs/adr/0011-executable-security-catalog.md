# ADR 0011: Executable security catalog

## Status
Accepted

## Decision
Represent controls, detectors, ABAC policies, IR playbooks, and privacy rules as Spring components with stable IDs.

## Consequences
Catalog grows with meaningful commits; runtime can query/evaluate definitions instead of static PDFs only.
