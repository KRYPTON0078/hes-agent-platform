# Contributing

1. Use JDK 21 and Maven 3.9+
2. Keep commits focused and English
3. Run `mvn test` before pushing
4. Do not commit secrets or local `.env`
5. Prefer small PRs mapped to one concern (protocol, DB, ops, docs)
